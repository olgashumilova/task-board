import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#48a999",
      main: "#00796b",
      dark: "#004c40",
      contrastText: "#ffffff",
    },
    secondary: {
      light: "#48a999",
      main: "#00796b",
      dark: "#004c40",
      contrastText: "#ffffff",
    },
  },
});

export default theme;
